# 2026-06-23

## Заметки

- Сессии Claude Code сохраняются в `~/.claude/projects/**/*.jsonl` — можно читать из другой сессии
- Handover: project note в `Projects/` → в новой сессии "продолжи по [файл]"
- Запускать сессию из папки проекта, иначе транскрипт попадёт не туда
- MS Graph: перешли с PowerShell на Python (msal + device code flow) → [[ms-graph-setup]]

## MS Graph сессия — итог

Подняли полноценный инструмент чтения почты M365 из Claude Code CLI.

**Сделано:**
- Авторизация через device code flow — токен закеширован (`~/.ms_graph_token_cache.json`)
- Скрипт `fetch_mail.py` перенесён в vault: `Tools/Scripts/fetch_mail.py`
- Создана команда `/mail` (`~/.claude/commands/mail.md`)

**Возможности `/mail`:**
- `/mail` / `/mail вчера` / `/mail 2026-06-22` — письма за дату
- `/mail --search "Canonical PO"` — полнотекстовый поиск (KQL)
- `/mail --from brandon` — по отправителю
- `/mail --from brandon --thread` — переписка в обе стороны (входящие + исходящие)
- `/mail --attachments` — скачать аттачменты в `~/Downloads/mail-attachments/`
- `/mail --since` / `--until` — диапазон дат

**Протестировано:** поиск по brandon → 17 писем Connectivity Point / Granozzo, включая письмо от сегодня 14:41 с запросом на site visit во вторник-среду следующей недели.

**Gitignore:** плагины Obsidian выведены из трекинга.
